name: Build Multi-Platform Installers

permissions:
  contents: write
  
on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Build runtime image
        run: |
          jlink --add-modules java.base,java.net.http --output runtime --compress=2

      - name: Package application (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          jpackage --name IPFinder --input . --main-jar IPFinder.jar --runtime-image runtime --type exe --app-version 1.0

      - name: Package application (macOS / Linux)
        if: matrix.os != 'windows-latest'
        run: |
          jpackage \
            --name IPFinder \
            --input . \
            --main-jar IPFinder.jar \
            --runtime-image runtime \
            --type ${{ matrix.os == 'macos-latest' && 'dmg' || 'deb' }} \
            --app-version 1.0

      - name: Upload artifacts (modern v4)
        uses: actions/upload-artifact@v4
        with:
          name: IPFinder-${{ matrix.os }}
          path: |
            *.exe
            *.dmg
            *.deb
  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: IPFinder-macos-latest
          path: release/

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: IPFinder-windows-latest
          path: release/

      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: IPFinder-ubuntu-latest
          path: release/

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/**
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}